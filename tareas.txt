Hacer trriggers a la bd para:
1. Programacion de tractores:
    -Al agregar:
        --agregar la rutina.
    -Al editar:
        --editar la rutina.
    -Al anular:
        --eliminar la rutina.
2. Articulos por modelo de implemento:
    -Al editar:
        --editar a la tabla de componentes por modelo por cada implemento del modelo editado.
    -Al eliminar:
        --eliminar a la tabla de componentes por modelo por cada implemento del modelo eliminado.
3. Piezas por modelo de componente:
    -Al editar:
        --editar a la tabla de componentes por modelo por cada componente del modelo editado.
    -Al eliminar:
        --eliminar a la tabla de componentes por modelo por cada componente del modelo eliminado.
